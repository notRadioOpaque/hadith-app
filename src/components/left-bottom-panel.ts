export default function createLeftBottomPanel() {
  const leftBottomPanel = document.createElement("div");
  leftBottomPanel.className = "w-full h-[10%] bg-white flex items-center justify-between px-6";

  return leftBottomPanel;
}